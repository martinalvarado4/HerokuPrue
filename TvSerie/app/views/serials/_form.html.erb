<%= form_for(serial) do |f| %>
  <% if serial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serial.errors.count, "error") %> prohibited this serial from being saved:</h2>

      <ul>
      <% serial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :genero %>
    <%= f.select :genero, ["Accion", "Suspenso", "Drama", "Anime", "Romance", "Terror"] %>
  </div>

  <div class="field">
    <%= f.label :año %>
    <%= f.number_field :ano %>
  </div>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_area :titulo %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
